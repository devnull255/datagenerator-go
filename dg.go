package main

import (
       "fmt"
       "math/rand"
)


func main() {
   // Create and seed random generator
   // below uses fixed seed, will change to use date and time
   // to guarantee different output per run
   r := rand.New(rand.NewSource(99))

}
